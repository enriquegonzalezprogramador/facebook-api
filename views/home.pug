html
	head

		link(rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css")

		link(rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/flat-ui/2.3.0/css/flat-ui.css")

	body
		.container(style={background: '#ECEFF1', width:'100%'} )
			nav.navbar.navbar-inverse.navbar-embossed
				.container-fluid
					.navbar-header
						a.navbar-brand(href="#") PlayBetcris
					.collapse.navbar-collapse
						ul.nav.navbar-nav.navbar-right
							li
								a(href="/friends") Buscar mis amigos
							li
								a(href="/auth/close") Cerrar sesión
			section
			h1 #Promo

			p Venezuela vs Peru
			.row
				.col-xs-6(style="float:none;")
					form(action="/move" method="POST")
						input(type="text" class="form-control" placeholder="Ingresa tu pronostico" name="pronostico")
						input(type="submit" value="Ingresar Pronostico" class="btn btn-success" style="margin-top:20px;")